/**
 * Storage helper placeholder. Future implementations will read/write data that
 * conforms to `contracts/state/*.schema.json` and applies the defaults described
 * in `docs/brief.md` and `spec/system-capabilities.md`.
 */

export function getSessionStateKey(): string {
  return 'codex.tasks.state';
}
